"""Setup configuration for tvsm-smad.

This file exists for backwards compatibility with older pip versions.
All dependencies are now managed via pyproject.toml.
"""

from setuptools import setup

setup()
